APP_NAME=ENSTO Sales Tool API
ENVIRONMENT=local
# In Docker Compose the hostname is `db`. When running uvicorn on the host, use localhost:
# DATABASE_URL=postgresql+psycopg://ensto:ensto@localhost:5432/ensto_sales_tool
DATABASE_URL=postgresql+psycopg://ensto:ensto@db:5432/ensto_sales_tool

